import 'package:flutter/material.dart';

extension AppColors on ColorScheme {
  // 리스트(컨테이너) 배경 컬러
  Color get fieldBackground => Color(0xFFFFFFFF);
  // 홈 위치,메세지 전송 박스 배경 컬러
  Color get buttonBackground => Color(0xFF983E24);
  Color get selectedBorder => Color(0xFF983E24);
  // 리스트(컨테이너) 외곽선 컬러
  Color get fieldBorder => Color(0xFFD2D5DA);
}
